@model Cdmx.Scg.Sancionados.Web.Models.ViewModels.RecuperaPassMV
@{
    ViewBag.Title = "Recuperar";
    Layout = "~/Views/Shared/_InicioLayout.cshtml";
}

<h2>Recuperar Contraseña</h2>
@using (Html.BeginForm("Recuperar", "Cuenta", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Ingrese su Correo</h4>

    @*@Html.LabelFor(m => m.Email, htmlAttributes: new { @class = "control-label" })
    <div class="input-group">
        <span class="fa fa-mail-bulk" aria-hidden="true"></span>
        @Html.EditorFor(m => m.Email, new { htmlAttributes = new { @class = "form-control", placeholder = "Ingresa tu correo " } })
    </div>
    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })*@

    @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "control-label" })
    <div class="input-group">
        <span class="fa fa-mail-bulk" aria-hidden="true"></span>
        @Html.PasswordFor(d => d.Email, new { htmlAttributes = new { placeholder = "Ingresa tu correo" } })
    </div>
    @Html.ValidationMessageFor(d => d.Email, null, new { style = "color:red;" })



    @*<div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Enviar Correo" class="btn btn-success" />
            </div>
        </div>*@
    <div class="py-3">
        <button class="btn btn-primary btn-block btn-submit" type="submit">Enviar Correo</button>
    </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}